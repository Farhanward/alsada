# Changelog — alsada

## 1.0.0 — 2026-07-05 (الترقية المؤسسية)

- **خدمة HTTP محلية**: `python -m alsada.cli serve` — `GET /api/client` (بروفايل العميل وعدد البرومبتات) و`POST /api/analyze` (تحليل إجابة AI: ذكر/بروز/نبرة/منافسون/استشهادات/تنبيهات) + `/api/health` و`/api/version` و`/api/metrics`.
- **قرار أمني**: الاستجواب الحي (`run/probe`) يبقى CLI-only لأنه قد يستهلك مفاتيح/أرصدة مزودين خارجيين.
- **Config موحد**: `alsada/config.py` يجمع مسارات المشروع ودوال `load_client/load_prompts` الأصلية + إعدادات الخدمة عبر البيئة (`ALSADA_*`).
- **Observability**: سجلات JSON بتدوير تلقائي + عدادات وp50/p95/p99 عبر `/api/metrics`.
- **مصادقة وحدود**: `X-API-Key` بمقارنة constant-time، وحد حجم الطلب (413) مع تفريغ آمن للجسم.
- **اختبارات enterprise**: 14 اختباراً جديداً فوق اختبارات الـpipeline الـ11 (المجموع 25).

## 0.x — 2026-06-27 → 2026-07-04 (تاريخ ما قبل الترقية، موثق في MASTER_REFERENCE)

- MVP المراقبة: استجواب 7 محركات (mock/ollama/openai/openrouter/perplexity/anthropic/file) → تحليل → درجة ظهور.
- قياس حقيقي لـ CarbonFlow (31.8/100 ثم 25/100 عبر perplexity/sonar) وكشف تسمية منافس بدل العلامة.
- المرحلة 2: `generate` (llms.txt + schema.jsonld + صفحات) و`compare` (إثبات الحركة) — ونُشر تحديث الموقع فعلياً 2026-06-29.
- «الرادار»: ترتيب منافسين + مصادر استشهاد + فرص. «الناشر»: خطة نشر مصنفة آمنة بلا سبام.
- مركز النشر الاجتماعي (`social.py`) + مهمة قياس أسبوعية مجدولة على Windows.
- تشديد المحلل (2026-07-04): لا تحسب كلمة عامة كمنافس إلا بإشارة علامة واضحة، ولا يقبل نطاق مزيف كاستشهاد.
