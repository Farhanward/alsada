# مركز النشر الاجتماعي — الإعداد والتشغيل

نشر محتوى carbonflows.store إلى **X (تويتر) · إنستغرام · تيك توك** عبر **APIs الرسمية**، تملكه أنت (يعمل على سيرفرك)، ومربوط بـ«الناشر».

```
الناشر (يولّد المحتوى) ─▶ الطابور (social_queue) ─▶ social.py ─▶ X / Instagram / TikTok
```

## التشغيل
```bash
cd C:\Projects\alsada
python -m alsada.social status                 # حالة كل منصّة (creds موجودة؟)
python -m alsada.social post --text "..." --link https://www.carbonflows.store
#   ^ وضع تجريبي افتراضي (لا ينشر). أضِف --live للنشر الفعلي بعد إضافة المفاتيح.
python -m alsada.social post --text "..." --queue     # أضِف للطابور
python -m alsada.social publish-queue --live          # انشر كل الطابور فعلياً
```
**كل شيء يعمل الآن في الوضع التجريبي بلا مفاتيح** — فتختبر خط الأنابيب كاملاً قبل الحصول على صلاحيات API.

## أسرع مسار للنشر اليوم (بدون انتظار موافقات API)
استخدم أداة جدولة جاهزة كجسر مؤقت: **Metricool** أو **Publer**. الهدف هنا ليس استبدال `social.py`، بل تشغيل النشر فوراً بينما تظل مفاتيح X/Meta/TikTok والمراجعات الرسمية قيد التجهيز.

```
الناشر (يولّد المحتوى) ─▶ مراجعة بشرية سريعة ─▶ Metricool/Publer ─▶ X / Instagram / TikTok
                                  │
                                  └── نفس المحتوى يبقى قابلاً للنقل لاحقاً إلى n8n/social.py
```

### لماذا هذا المسار؟
- تنشر من لوحة واحدة اليوم بعد ربط الحسابات مرة واحدة.
- تتجاوز تأخير مراجعات تطبيقات المطورين، خصوصاً TikTok وMeta.
- تحافظ على `alsada/social.py` كمسار مملوك لاحقاً، بدل رمي العمل الحالي.
- تجعل n8n يستلم نفس خطة النشر لاحقاً: جدولة يومية → قراءة الطابور → نشر عبر APIs الرسمية أو عبر Publer API إن اخترته كجسر.

### خطة تشغيل اليوم
1. افتح حساب Metricool أو Publer واربط حسابات CarbonFlow: X، Instagram، TikTok.
2. خذ منشورات `generated/publish/owned/` وحوّلها إلى 7-10 منشورات قصيرة.
3. خصّص النص لكل منصة: X أقصر، Instagram بصري/CTA، TikTok فيديو قصير أو سكريبت.
4. جدْول أسبوعاً كاملاً يدوياً من اللوحة.
5. احتفظ بنسخة النصوص في الطابور المحلي عبر:
```bash
python -m alsada.social post --queue --text "<النص المنشور>" --link https://www.carbonflows.store
```

### قرار عملي
- اختر **Metricool** إذا كان التركيز الآن على التخطيط والتحليلات ومعرفة أفضل أوقات النشر.
- اختر **Publer** إذا أردت جسراً أسهل لاحقاً مع الأتمتة، لأنه يوفّر API للنشر يمكن ربطه بـ n8n عند الحاجة.

## 🔑 الحصول على المفاتيح (خطوات تقوم بها أنت مرّة واحدة)
ضعها في `C:\Projects\alsada\.env.local`:

### X (تويتر)
1. `developer.x.com` → أنشئ Project + App.
2. فعّل **OAuth 2.0**، صلاحيات **tweet.write + tweet.read + users.read**.
3. احصل على **User Access Token** (سياق المستخدم).
4. `X_ACCESS_TOKEN=...`
> الخطة المجانية تسمح بنشر محدود؛ للنشر المكثّف تحتاج خطة Basic.

### إنستغرام (Graph API)
1. حوّل حسابك إلى **Business/Creator** واربطه بصفحة فيسبوك.
2. `developers.facebook.com` → أنشئ App (نوع Business) → أضِف **Instagram Graph API**.
3. احصل على **IG User ID** + **Long-lived Page Access Token**.
4. `IG_USER_ID=...` و `IG_ACCESS_TOKEN=...`
> إنستغرام يتطلّب **رابط صورة عام** (image_url). ارفع الصور على R2/الموقع.

### تيك توك (Content Posting API)
1. `developers.tiktok.com` → أنشئ App → اطلب **Content Posting API** (يحتاج مراجعة/موافقة).
2. OAuth بصلاحية `video.publish`.
3. `TIKTOK_ACCESS_TOKEN=...`
> تيك توك يتطلّب **رابط فيديو عام** (video_url).

## النشر للسيرفر (الخيار «تملكه أنت»)
- انسخ مجلد `alsada` إلى السيرفر، وضع `.env.local`، وأضِف **cron** أو **n8n** ينادي `publish-queue --live` يومياً.
- أو في **n8n** على سيرفرك: عقدة جدولة → عقدة HTTP/Twitter → تقرأ من نفس الطابور. (X له عقدة n8n جاهزة؛ IG/TikTok عبر عقدة HTTP بنفس الطلبات أعلاه).

## 🔗 الربط مع «الناشر»
مخرجات `alsada publish` (المحتوى المملوك) + `alsada radar` (المواضيع/المصادر) → تُحوَّل لمنشورات اجتماعية وتُدرَج بالطابور:
```bash
python -m alsada.social post --queue --text "<من محتوى الناشر>" --link https://www.carbonflows.store
```

## 🧱 محاور المحتوى (3 منشورات/أسبوع لكل منصّة)
1. **نصيحة أتمتة سريعة** · 2. **قبل/بعد لعملية أُتمتت** · 3. **عرض منتج** · 4. **بصيرة AI بالعربي** · 5. **نتيجة عميل**.

## ✍️ منشورات افتتاحية جاهزة
1. «توقّف عن نسخ البيانات يدوياً. ربطنا متجراً بـ n8n فصار يرسل الطلبات لواتساب تلقائياً خلال 5 دقائق. أتمتة عملك = وقت ومال موفّر. 👇 carbonflows.store»
2. «بياناتك أغلى من أن تذهب لسحابة الغير. نبني أنظمة ذكاء اصطناعي **ذاتية الاستضافة** تبقى عندك. الخصوصية ليست رفاهية. 🔒»
3. «3 مهام في شركتك يمكن أتمتتها هذا الأسبوع: متابعة الطلبات، الردّ الأولي على العملاء، التقارير اليومية. نوريك كيف. ⚡»

> القاعدة: محتوى قيمي حقيقي، APIs رسمية، لا سبام. (يتماشى مع مبدأ «الناشر»).
